apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dcae-datalake.fullname" . }}-test-connection"
  labels:
{{ include "dcae-datalake.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "dcae-datalake.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
